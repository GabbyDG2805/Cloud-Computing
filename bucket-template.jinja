#Template to create bucket storage in a given region

resources:
- name: {{ env["name"] }}
  type: storage.v1.bucket
  properties:
    location: {{ properties['region'] }}
    kind: storage#bucket
    storageClass: {{ properties['storageClass'] }}
    versioning:
      enabled: false